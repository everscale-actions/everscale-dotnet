<Project>

    <PropertyGroup>
        <!--    TON SDK version -->
        <!--    used by tools\ClientGenerator (tools/api.json) -->
        <!--    used by src\ch1seL.TonNet.RustAdapter (runtime binaries) see DownloadLib.targets -->
        <SDKVersion>1.2.0</SDKVersion>
    </PropertyGroup>
    
</Project>