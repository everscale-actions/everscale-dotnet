<Project>

    <PropertyGroup>
        <!--    TON SDK version -->
        <!--    used by tools\ClientGenerator (tools/api.json) -->
        <!--    used by src\EverscaleNet.RustAdapter (runtime binaries) see DownloadLib.targets -->
        <!--    used by src\EverscaleNet.Adapter.Wasm (runtime binaries) see DownloadLib.targets -->
        <SDKVersion>1.28.0</SDKVersion>
    </PropertyGroup>

</Project>